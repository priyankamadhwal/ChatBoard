{
  "name": "chatboard",
  "version": "1.0.0",
  "description": "A real-time chat application with that performs semantic analysis on every incoming message.",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "postinstall": "(cd server && npm install); (cd client && npm install)"
  },
  "author": "Priyanka Madhwal",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.2.0",
    "react-gauge-chart": "^0.2.5"
  }
}
